{
  "name": "crawler-study",
  "private": true,
  "version": "0.0.1",
  "main": "./build/app.js",
  "scripts": {
    "dev": "concurrently \"tsc -w\" \"nodemon\"",
    "build": "rm -rf build && tsc",
    "start": "nodemon"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.12",
    "puppeteer": "^15.5.0",
    "queue": "^6.0.2"
  },
  "devDependencies": {
    "concurrently": "^7.3.0",
    "nodemon": "^2.0.19",
    "pm2": "^5.2.0"
  }
}
